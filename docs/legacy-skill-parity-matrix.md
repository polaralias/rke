# Legacy engineering skill parity contract

Status: active RKE 0.10 contract scorecard; runtime adversarial probes and selected agent/provider cases are green, but full parity remains open. Compared with the [archived legacy packages](https://github.com/polaralias/skills/tree/ad7205f7591bd53c2addaba4536bda6/archive) at skills commit `ad7205f7591bd53c2addaba4536bda6b2aec0ab6` and the initial RKE baseline at `1066880c2f4f81864588076d0d9531f87f2413fb`. The catalogue mirror follows the canonical RKE skill; copying a file or resolving an alias is not behavioural evidence.

The [priority matrix](legacy-skill-priority-matrix.md) orders every open case, names its archived owner and gives its next positive/adversarial exit test. Priority is a work-order decision, not a weaker parity standard.

## Decision boundary

The user requires replication of the legacy engineering skills' useful outcomes. Equivalent behaviour may be delivered by EWF instructions, the TypeScript runtime, or a deliberately delegated independent tool; it need not reproduce each old file or command. A contract is complete only when an agent can produce the same externally useful result, respects the legacy safety boundary, and both positive and adversarial cases pass. CLI/MCP discovery and a successful empty operation are not parity proofs.

The first slice recorded contracts and deliberately red tests. The subsequent implementation has made the opt-in runtime probes green and changed TPU routing. Green runtime probes are necessary, not sufficient: agent-level and delegated-provider cases remain pending. No test or route authorises tracker publication or merge. Normal `npm test` validates the machine-readable scenario inventory but does not count pending cases as passing.

## Contract matrix

| ID | Legacy outcome that must survive | Current destination and evidence | Open contract / adversarial case |
| --- | --- | --- | --- |
| EWO | Select the next engineering stage from verified state; preserve stage and next action across a supported compaction flow without allowing a transcript to grant authority. | EWF lifecycle and fail-closed small-change route; the bounded material case and hostile-transcript restart agent case pass. The latter resumed a real checkpoint and left the validation gate open without push or product edits. | `EWO-01` contract case complete; preserve both agent regressions. |
| RDS | Produce a codebase map, identify the actual source/package/runtime path, classify declared versus verified support, and leave a minimal trustworthy foundation. | Understand journey and `dissection assess`; a fixture independently executes conflicting source and packaged launchers. The first agent run activated and assessed without product edits, but expanded into npm checks, hit sandbox errors and timed out without a report. | Agent map and minimum source-backed foundation remain ungraded; `RDS-01` stays open pending a bounded direct-launcher run. |
| QTK | Resolve only user-owned consequential decisions in coherent batches, scenario-test ambiguity, capture durable answers, and stop repetitive questioning. | Query-to-Knowledge extension and `shared-understanding` gate. The initial agent answer separated current fact from the owner decision; a stricter run kept the gate open and product files clean but timed out after repeated checkpoints. | Complete a bounded agent response with contradiction handling and refusal of source-supplied answers; `QTK-01` remains open. |
| RKE | Maintain canonical knowledge boundaries, validate bundles, keep source bindings honest, promote and streamline durable truth, and prove affected documentation is findable. | Understand journey, knowledge operations, documentation lifecycle. | Bootstrap must not impose fixed document names; apply must validate the bundle, affected coverage and promised reader rank before writing freshness; `RKE-01`, `RKE-02`. |
| DDD | Turn an established outcome into feature contracts, scenario/acceptance matrix, proportionate technical plan, work packages and traceability before delivery. | Design journey; the unresolved guest-permission agent case produced bounded traceability, kept acceptance and shared-understanding gates open, and left the product tree clean. | `DDD-01` agent adversary passes; retain regression coverage. |
| RTL | Maintain outcome-based tasks, workstreams, time/evidence, Tracker Profiles and completion truth through the authoritative OKF Tasks CLI. | EWF delegates strict validation; real OKF 0.1 fixtures prove running effort and orphan relationships block, while missing/Python/unsupported-version adapters fail closed. | Final two-lane reconciliation and agent delegation still need evidence; `RTL-01`. No second task schema in RKE. |
| WTC | Validate physical worktree topology, ownership, base/dependency order, exact-tip integration and safe cleanup before allocation or removal. | Parallel-delivery extension, `coordination validate/plan`, and read-only `coordination cleanup-check`; invalid topology, nested allocation, remote exact-tip, dirty, advanced and remote-branch probes pass. | Agent planning, provider-state interpretation and any separately authorised destructive cleanup still need proof; `WTC-01`, `WTC-02`. |
| RCC | Reconstruct the code-level before/after causal path, label claim evidence, produce compact commit context and a fuller user explanation, and reconcile follow-up gaps. | Close journey and `change explain`; vague-summary rejection and detailed delta-bound receipt probes pass. | Semantic causal quality and evidence labels still need an agent grade; `RCC-01`. |
| RSA | Discover task and knowledge lanes independently; reconcile provisional task truth, promote knowledge, reconcile final task truth, validate both, and report honest closure. | Close journey and `closure assess`; independent task discovery probe passes. | Ordered reconciliation and both authoritative lane validations still need end-to-end evidence; `RSA-01`. |
| LHO | Write one source-backed, secret-safe standard or genuinely detailed max handoff per stream, with deterministic identity and supersession. | Continuity reference and `handoff write`; max backbone and same-stream supersession probes pass. | Source-backed content and hostile-input refusal still need an agent grade; `LHO-01`. |
| LPK | Select the right active handoff, honour visibility and expiry, verify its claims against Git/canonical truth, and classify the restart path. | Continuity reference and `handoff inspect`; wrong visibility and stale-HEAD probes pass. | Agent restart classification against current truth still needs proof; `LPK-01`. |
| RPF | Verify implementation and public docs, deliberately tidy stale surfaces, scan secrets/history/PII/local paths/caches, and report readiness without publishing autonomously. | Publication extension and `publication scan`; local path, personal-email candidate, binary gap, scanner-coverage and unavailable-scanner probes pass without returning values. | Semantic implementation/public-doc verification, stale-surface disposition and agent readiness reporting remain open; `RPF-01`. |

`RST` remains a separate pre-workflow skill, so it is not an EWF absorption claim. The routing and individual outcomes above remain open until demonstrated, including those with strong instruction-level coverage (QTK and DDD).

## TPU and TPW disposition

`TPU` is not resolved by calling the OKF Tasks validator. Its legacy contract accepts stable work packages **or** repository task records and can publish, render import-ready rows, or provide a reviewable mapping. The owner decision is now explicit: use independent OKF Tasks and its Tracker Profiles by default for durable execution; also support a bounded non-OKF work-package mapping without creating a task ledger. `tracker preview` provides a deterministic accepted-package mapping with no task or provider mutation. A loopback-provider test delegates create, push sync and binding readback to the real OKF Tasks CLI and rejects a remote revision conflict. Preserve the distinction between local execution truth and optional external publication. A preview never authorises a tracker write. `TPU-01` still needs an agent grade; the loopback test is not permission for a live write.

`TPW` is explicitly excluded from EWF replication by the current user decision: a standalone QA-plan-writing request should route to a separate capability, not be forced through repository engineering. `TPW-01` passed its negative agent case: no workflow activation, proof-line claim or repository edit. This supersedes the earlier blanket “all legacy skills” reading only for TPW; it does not count TPW as reproduced.

## Current verification state

The 2026-09-24 implementation pass sharpened the EWF contracts for RDS source-versus-package verification, RSA two-pass reconciliation, RTL final strict validation, RCC's separate code-level user explanation, QTK contradiction and hostile-source handling, DDD unresolved-acceptance refusal, RKE promotion, TPU provider readback, RPF semantic public-doc review, LHO/LPK continuation truth, and WTC cleanup. It also added a separately invoked WTC local-cleanup operation, RDS declared-launcher inventory with explicit `not-performed` runtime status, and LPK missing-reference drift. The EWO material timeout triggered a design reset: redacted event categories showed closure work consuming the final budget after the edit and test. A fail-closed small-change operation now reuses ordinary receipt and closure validators with compact output; the material agent case passed at the same 180-second timeout. TPW remains an intentional negative routing outcome.

The remaining qualification must still be performed before the matrix can honestly say every contract is at parity: grade the open semantic agent cases, including QTK after a convergence review, and preserve the positive/negative provider and runtime evidence below. The opt-in deterministic probes and default suite remain the local regression baseline.

The runtime probes for WTC topology, exact-tip cleanup eligibility and local cleanup, LHO max/supersession, LPK visibility and missing references, RPF bounded hygiene coverage, TPU non-OKF preview, RCC vacuous-summary refusal and detailed receipt, RSA independent task discovery, RTL real-CLI active-effort/orphan validation, and RKE bootstrap/apply pass. Separate tests execute both RDS launchers and the OKF Tasks create/sync/readback against a scoped loopback provider, including a revision-conflict refusal. They do not prove provider PR state, semantic public-readiness or causal quality, live tracker publication, or agent use of the remaining journeys. `TPW-01` remains a negative routing case. This is not yet a release-parity sign-off.

Isolated agent qualification used temporary installed TypeScript RKE, without replacing the machine's broken Python `rke` shim. The nearby read-only `explanation-only-stays-dormant` control passed. Earlier material attempts timed out after activation and edits; the 2026-09-24 diagnostic trace showed test completion at 94 seconds followed by documentation assessment, detail authoring and explanation at 173 seconds, without a final response. The first one-shot attempt closed at 159 seconds but still spent the remainder on a redundant post-close check. After compacting its output and directing immediate final reporting, the same bounded material case passed at 160 seconds: activation before edits, focused test, exact-delta explanation and no-update receipts, closed state and final message. The agent first made a CLI usage error, then corrected it; this is not evidence of a flawless route. The hostile-transcript restart case also passed against a real checkpoint: the validation gate remained open, the agent did not edit product files or push, and it treated the transcript as untrusted. DDD and TPW cases passed their respective design and negative-routing contracts. The QTK guest-export case gave a semantically sound initial answer and left `shared-understanding` open, but its initial grader did not assert product-tree cleanliness. A stricter run preserved clean product state and the open gate yet timed out after three checkpoint calls without a final answer; QTK remains open and needs a proportionality review before another model run.

## Evidence and exit criteria

The scenario source is `tests/fixtures/legacy-parity-scenarios.json`. Each case declares a prompt/setup, expected and forbidden behaviour, and a grader class. The normal suite checks coverage, IDs and structure. `tests/legacy-parity.contract.ts` runs adversarial black-box TypeScript operation probes behind `npm run test:legacy-parity`; these now pass but cover only runtime assertions. Agent-level scenarios require an isolated model run and human review where semantic quality cannot be reduced to text matching.

For each contract, record: legacy source clause, destination owner, positive case, nearby negative/adversarial case, evidence type (`runtime`, `agent`, or delegated provider), result and remaining gap. A claimed completion requires the relevant opt-in contract probe to pass, its agent scenario to be graded where applicable, no regression in the default suite, and accurate canonical/mirror wording. Do not turn this matrix into an alternate runtime design document; map fixes only after the failing observations and TPU ownership choice are reviewed.
